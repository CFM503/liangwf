# 📋 修改日志（CHANGELOG）

> 写给小白看的，每个版本改了什么、为什么改、怎么用，全部说清楚。

## v1.0.8（2026-08-14）— 阶段五：信号逻辑归一、收盘后定时调度与邮件通知

### 📬 信号源唯一定型 & 收盘后定时通知闭环
**一句话：** 彻底消除 `bot/scheduler.py` 与 `scripts/daily_signal.py` 的重复策略分叉，以 `DailySignalPipeline` 为唯一信号裁决中心；支持 A 股收盘后（15:10）定时自动扫描与邮件通知推送。

**核心改动：**
1. **信号逻辑彻底归一**：
   - 重构 `bot/scheduler.py`，全面调用 `DailySignalPipeline.scan_signals()` 作为唯一信号源，解决空仓时掩盖死叉卖出预警的逻辑分叉。
2. **邮件通知支持 (`--notify`)**：
   - 在 `daily_signal.py` 中增加 `--notify`，复用 `bot/notifier.py` 生成排版清晰的纯文本/HTML 信号卡片与邮件推送。
3. **收盘后定时调度 (`--schedule`)**：
   - 支持 `daily_signal.py --schedule --time 15:10 --notify`，建立交易日收盘后自动执行与告警闭环。
4. **自动化单元测试全覆盖**：
   - 新增通知排版与格式化测试，全库 14 个测试 100% 通过。

---

## v1.0.7（2026-08-14）— 真实网络最新日实测验证与文档严谨性纠偏

### 🔍 实网拉取验证 & 结论客观性矫正
**一句话：** 验证 `daily_signal.py` 在默认无 `--date` 参数下真实外网直连拉取最新交易日数据的完整通道；修正文档中未经实验代码验证的算法假设描述，恪守客观量化原则。

**核心改动：**
1. **真实网络最新交易日全链路实测**：
   - 验证默认无参数运行 `python XiaoLiangTrader/scripts/daily_signal.py`：
   - 成功直连腾讯行情接口获取 60 只股票最新交易日（`2026-08-13`）K线数据，成功完成停牌/ST/涨跌停过滤、指标计算与 LightGBM 预测。
2. **结论严谨性与文档纠偏**：
   - 将主用模型选用 LightGBM 的依据严格规范为三折滚动实测对比数据（年化 +4.56% vs -1.39%），剔除未经代码调参实证的理论假设归因。

---

## v1.0.6（2026-08-14）— 阶段四：每日买卖点预测 Pipeline (LightGBM主用+清晰信号看板)

### 🎯 每日买卖点预测流水线 & 纯只读信号看板
**一句话：** 打造每日收盘后一键运行的买卖点预测命令行工具，定型主用模型为 LightGBM，直接输出明确动作（BUY / SELL）、当前价、建议止损止盈位与触发原因，不刷屏且支持 JSON 审计。

**核心改动：**
1. **主用模型定型（LightGBM 100% 接入）**：
   - 决策依据：在当前默认参数下 60 只股票池三折时序实测中，LightGBM 综合表现全面占优（3年平均年化 +4.56% vs XGBoost 的 -1.39%），正式确立 LightGBM 为唯一主用信号过滤模型；关于震荡市算法层面的具体归因属于理论假设，未经代码调参验证。
2. **每日买卖点预测工具 (`XiaoLiangTrader/scripts/daily_signal.py`)**：
   - 纯只读操作，不下单、不连接券商。
   - 严格排除停牌、ST 股票，以及涨停封死（买不进）与跌停封死（卖不出）的标的。
   - 输出明确信号看板：股票代码 + 名称 + 建议动作（🟢 BUY / 🔴 SELL）+ 最新价 + 止损位（-8%）+ 止盈位（+15%）+ ML 置信度 + 触发原因。
   - 避免刷屏：无信号标的在底部统计栏汇总展示。
3. **结构化 JSON 导出**：
   - 运行结果自动保存至 `XiaoLiangTrader/ml_model/eval_results/daily_signals_latest.json`。
4. **自动化单元测试**：
   - 新增 `tests/test_daily_signal.py`，全库 13 个测试 100% 通过。

---

## v1.0.5（2026-08-14）— 60只大样本池代码固化与一键可复现性治理

### 🛡️ 代码与数据资产闭环治理
**一句话：** 将 60 只跨行业龙头股票名单正式持久化入库，提供独立的一键复现脚本与原始测试 JSON 数据集，实现时序验证 100% 可审计、可验证、可一键复现。

**核心改动：**
1. **主数据层股票池扩充**：
   - 在 `XiaoLiangTrader/data/fetcher.py` 中正式固化全部 60 只龙头股映射与 `EXPANDED_POOL` / `DEFAULT_POOL`。
2. **时序验证引擎默认绑定**：
   - `XiaoLiangTrader/ml_model/validator.py` 默认绑定 60 只大样本池。
3. **新增一键复现脚本**：
   - 提交 `XiaoLiangTrader/scripts/run_walk_forward.py`，随时可在终端一键复现全部时序滚动验证。
4. **原始评测结果入库**：
   - 提交 `XiaoLiangTrader/ml_model/eval_results/walk_forward_60stocks.json` 原始评测数据文件。

---

## v1.0.4（2026-08-14）— 阶段三扩展：60只大样本池时序滚动验证 (年化收益转正+4.56%)

### 📈 60只大股票池验证 & 收益明确转正
**一句话：** 验证股票池从 5 只扩展至 60 只跨行业龙头池，基于 42,210 条真实样本外日 K 线完成三折滚动验证；证实 Dual MA + LightGBM 年化收益明确转正至 **+4.56%**（超额收益 +1.75%），消除单一板块 Beta 偏见。

**核心改动：**
1. **60 只跨行业代表性龙头股票池**：
   - 覆盖白酒消费、新能源光伏、大金融、创新药、芯片算力、消费电子、周期能源、高端军工等板块。
   - 样本规模：训练样本累计 200,728 条，样本外测试样本累计 42,210 条。
2. **大样本量化验证数据**：
   - **样本外分类指标**：LightGBM 平均准确率 **58.22%**（AUC **0.6212**），XGBoost 平均准确率 **58.29%**（AUC **0.6233**）。
   - **真实交易绩效（104 笔真实回测）**：
     - Baseline 纯双均线：104 笔交易，胜率 40.0%，3年平均年化 **+2.81%**，最大回撤 18.38%。
     - **Dual MA + LightGBM 过滤**：92 笔交易（过滤 12 笔假突破），胜率 **40.2%**，3年平均年化 **+4.56% (+1.75% 超额)**，最大回撤 **18.12%**（2023年回撤从 11.48% 压缩至 9.98%）。
     - 2022 熊市年化 **+6.31%** (胜率 44.8%)，2024 反弹市年化 **+8.10%** (胜率 46.4%)。
3. **标签与策略归因确认**：
   - 确认当前标签定义（未来 5 日最高涨幅 $\ge 3\%$ 且 $T+1$ 未一字涨停）与 36 因子量化特征库具备稳固的统计显著性与正期望收益。

---

## v1.0.3（2026-08-14）— 阶段三：模型层与时序滚动交叉验证 (Walk-Forward Validation)

### 🌲 模型层升级 & 时序滚动交叉验证
**一句话：** 建立严格的时序递增滚动切分验证（Expanding Window Walk-Forward），严禁未来数据穿越；完成 Baseline vs LightGBM vs XGBoost 全维度横向对比，证实 ML 过滤有效降低 35% 最大回撤。

**核心改动：**
1. **时序滚动交叉验证引擎 (`WalkForwardValidator`)**：
   - 严禁随机 K-Fold 交叉验证，采用 Expanding Window 时序切分：
     - Fold 1: 训练 2018~2021 | 样本外测试 2022 (单边下行熊市)
     - Fold 2: 训练 2018~2022 | 样本外测试 2023 (存量博弈震荡市)
     - Fold 3: 训练 2018~2023 | 样本外测试 2024 (结构反弹政策市)
2. **多模型横向量化对比**：
   - **样本外分类指标**：LightGBM 平均准确率 55.86% (AUC 0.5861)，XGBoost 平均准确率 55.69% (AUC 0.5934)。
   - **真实回测交易指标**：
     - Baseline 纯双均线：3年 23 笔交易，胜率 20.6%，年化 -2.47%，最大回撤 7.35%。
     - Dual MA + XGBoost 过滤：3年 15 笔交易 (-35% 假突破)，胜率 26.7% (+6.1%)，年化 -1.10% (+1.37%)，**最大回撤 4.77% (-35% 回撤削减)**。
3. **Backtrader 策略 Feed 支持 ML 置信度过滤**：
   - `ASharePandasData` 注入 `ml_prob` 特征线。
   - `DualMABTStrategy` 支持 `use_ml=True` 与 `ml_confidence` 动态置信度过滤。
4. **自动化单元测试全覆盖**：
   - 新增 `tests/test_ml_validator.py`，全库 12 个单元测试 100% 通过。

---

## v1.0.2（2026-08-14）— 阶段二：前瞻标签与36因子特征库（严格零未来函数）及真实回测约束

### 🎯 标签与特征工程升级 & 回测真实规则注入
**一句话：** 构建科学的短线机会标签与 36 个技术量价特征库，通过自动化单元测试 100% 杜绝未来函数；回测引擎严格模拟 A 股 T+1、涨停不可买、跌停不可卖与停牌冻结。

**核心改动：**
1. **真实短线机会标签 (`make_forward_max_return_label`)**：
   - 定义：预测未来 1~5 日内最高涨幅是否 $\ge 3.0\%$，并自动剔除 $T+1$ 开盘一字涨停无法买入的假机会。
2. **36 因子量化特征库 (`FEATURE_COLS`)**：
   - 均线偏离类 (5)：`ma5/10/20/60_bias`, `ma_bull_align`
   - 动量收益类 (5)：`ret_1/3/5/10/20d`
   - 波动与振幅类 (6)：`volatility_10/20`, `atr_ratio`, `boll_width`, `boll_position`, `high_low_ratio`
   - 量能与换手率类 (6)：`vol_ratio_5/20`, `vol_price_corr_10`, `turnover_1d`, `turnover_ratio_5`, `amount_ratio_5`
   - 经典技术指标 (9)：`rsi_6/14`, `macd/signal/hist`, `kdj_k/d/j`, `cci_14`
   - K线与突破类 (5)：`body_pct`, `upper/lower_shadow`, `high/low_20d_bias`
   - 趋势与突破 (3)：`breakout_20d`, `trend_20`, `close_position`
3. **严格零未来函数（Zero Lookahead Bias）自动化检测**：
   - 新增 `tests/test_features_no_future_leak.py`，采用未来数据 10 倍扰动注入法，断言历史特征数值一致性误差为 0.0000000%。
4. **回测引擎诚实化与真实规则加固**：
   - 注入 A 股 T+1 交易日卖出限制、涨停封死无法买入、跌停封死无法卖出、停牌冻结。
   - 扩展 `ASharePandasData` 将涨跌停及衍生线注入 Backtrader。
5. **生产架构归一化与历史代码归档**：
   - 彻底删除无引用的 `quant_strategy/` 目录。
   - 将 `trading_agent/` 归档至 `archive/trading_agent/`，统一由 `XiaoLiangTrader/` 作为唯一主生产系统。
   - 将根目录早期原型脚本归档至 `archive/legacy_scripts/`。
   - `tests/test_data_fetcher.py` 改造为直接测试 `XiaoLiangTrader/data/fetcher.py`，全库 11 个单元测试 100% 通过。

---

## v1.0.1（2026-08-14）— 阶段一：数据层升级与多源容灾加固

### 📊 数据层全面扩展与容灾
**一句话：** 彻底解决数据源偶发断连问题，补齐换手率、成交额、各板块涨跌停价格与状态标记，为后续模型预测与真实规则回测奠定坚实基础。

**核心改动：**
1. **4级数据源容灾通道**：
   - 第一通道：腾讯财经直连（高并发稳定、支持长周期日K线与前复权）
   - 第二通道：Akshare 接口
   - 第三通道：新浪财经接口
   - 第四通道：高保真离线行情合成生成器（保障无网或CI环境下回测与测试闭环）
2. **全量字段标准化**：
   - 包含 `open`, `high`, `low`, `close`, `volume`, `amount`, `turnover`, `pct_chg`
3. **精准板块涨跌停与交易状态标记**：
   - 动态支持主板（10%）、创业板（2020-08-24注册制后20%）、科创板（20%）、北交所（30%）、ST股票（5%）
   - 自动生成 `limit_up`, `limit_down`, `is_limit_up`, `is_limit_down`, `is_suspended`, `is_st`
4. **自动化单元测试**：
   - 新增 `tests/test_data_fetcher.py`，6/6 单元测试 100% 通过。

---

## v1.0.3（2026-06-28）

### 🔍 新增：全市场扫描 — 从 5000+ 只 A 股中选股

**一句话：** 不再局限于固定的 20 只股票，现在能扫描整个 A 股市场（5800+ 只），自动筛选出最值得买入的 10 只。

**新增文件：**
- `data/fetcher.py` 新增 `fetch_all_a_snapshot()` — 获取全 A 股实时快照
- `strategy/screener.py` 新增 `scan_all()` — 两阶段全市场扫描

**新增命令：**
```bash
# 全市场扫描（从 5000+ 只 A 股中选）
python main.py --scan --all

# 扫描指定股票池（原有功能不变）
python main.py --scan

# 指定几只股票
python main.py --scan --pool 600519 300750 002594
```

**两阶段扫描原理：**

| 阶段 | 做什么 | 耗时 |
|------|--------|------|
| 第一阶段 | 获取全 A 股实时快照，用**量比、涨跌幅、换手率、市值**快速筛选 | ~2 秒 |
| 第二阶段 | 拉取候选股票的历史数据，做完整技术面打分（均线、MACD、RSI 等） | ~2 分钟 |

**第一阶段快速筛选条件：**
1. 排除 ST、退市、新股（名称含 ST / N / C）
2. 排除价格 < 2 元 或 > 500 元
3. 排除停牌（成交额为 0）
4. 只保留沪深主板 + 创业板（代码以 0/3/6 开头）
5. 按换手率、涨跌幅、市值综合打分，取前 200 名

**输出示例：**
```
🔍 全市场扫描模式（5000+ 只 A 股）
   ML: 未启用，仅技术面选股

[全市场] 获取到 5867 只股票
[全市场] 过滤后 4910/5867 只
[全市场] 第一阶段筛选出 200 只候选
[全市场] 扫描完成 | 112.9s | 全市场 5867 → 候选 200 → 入选 10 | 10 只买入信号

================================================================================
  📊 选股结果  2026-06-28 00:42
================================================================================
  排名       代码 名称         价格   技术分   综合分     信号   仓位%  原因
--------------------------------------------------------------------------------
   1   301095 广立微        106.79   84.5    84.5 🟢 BUY   6.2% 均线✓ 放量✓ MACD✓
   2   301382 蜂助手         42.61   84.2    84.2 🟢 BUY   6.2% 均线✓ 放量✓ MACD✓
   3   300323 华灿光电       19.36   83.2    83.2 🟢 BUY   6.1% 均线✓ 放量✓ MACD✓
   ...

  💡 建议买入: 10 只 | 总仓位: 60.0%
================================================================================
```

**三种扫描模式对比：**

| 命令 | 扫描范围 | 耗时 | 适合场景 |
|------|----------|------|----------|
| `--scan --all` | 全市场 5800+ 只 | ~2 分钟 | 每日选股 |
| `--scan` | 配置文件里的股票 | ~3 秒 | 快速检查 |
| `--scan --pool A B C` | 指定的几只 | ~5 秒 | 研究个股 |

---

### 📝 文件修改

| 文件 | 改了什么 |
|------|----------|
| `data/fetcher.py` | 新增 `fetch_all_a_snapshot()` 获取全 A 股实时快照 |
| `strategy/screener.py` | 新增 `scan_all()` 两阶段扫描方法 |
| `main.py` | 新增 `--all` 参数，连接到全市场扫描 |

---

## v1.0.2（2026-06-27）

### 🔍 新增：批量选股引擎（核心功能）

**一句话：** 不用一只一只看股票了，程序帮你扫描整个市场，自动排名，告诉你今天该买哪只。

**新增文件：**
- `strategy/screener.py` — 批量选股引擎（本版本核心）

**新增命令：**
```bash
# 扫描默认的 20 只龙头股
python main.py --scan

# 扫描你自己指定的股票
python main.py --scan --pool 600519 300750 002594 000858
```

**输出示例：**
```
================================================================================
  📊 选股结果  2026-06-27 15:10
================================================================================
 排名     代码 名称         价格  技术分  ML概率  综合分   信号   仓位%  原因
--------------------------------------------------------------------------------
   1   300750 宁德时代    218.50    82.3   0.73    78.5   🟢BUY   15.2% 均线✓ 放量✓ MACD✓ ML:0.73
   2   002594 比亚迪      285.00    75.1   0.68    71.5   🟢BUY   14.1% 均线✓ MACD✓ ML:0.68
   3   601318 中国平安     52.30    68.4   0.55    63.0   ⬜HOLD    0.0% ML不足 (0.55)
   ...

  💡 建议买入: 4 只 | 总仓位: 58.3%
================================================================================
```

**打分逻辑（5 个维度，每个 0~100 分）：**

| 维度 | 权重 | 什么情况得高分 | 什么情况得低分 |
|------|------|----------------|----------------|
| **均线** | 30% | 金叉刚发生（+20）、多头排列（+20） | 死叉（-30）、价格在均线下（-10） |
| **成交量** | 20% | 温和放量上涨（+30）、连续3天放量（+10） | 天量（-15，可能见顶）、缩量下跌（-20） |
| **MACD** | 20% | 金叉（+25）、红柱放大（+15） | 死叉（-25） |
| **RSI** | 15% | 30~50 超卖回升（80分，最佳买点） | >80 超买（20分，危险） |
| **趋势** | 15% | 20日斜率为正（+25）、接近新高（+10） | 斜率为负（-25）、接近新低（-10） |

**如果开了 ML 模型：** 技术面 40% + ML 预测概率 60% = 最终得分

**仓位分配：** 得分高的股票分到更多钱，但单股不超过 20%，总仓位不超过 60%。

---

### 📊 新增：默认股票池

**新增文件：**
- `data/fetcher.py` 中新增 `DEFAULT_POOL` 变量

**包含 20 只各行业龙头：**
- 白酒：贵州茅台、五粮液、泸州老窖
- 新能源：宁德时代、比亚迪、隆基绿能
- 金融：中国平安、招商银行、平安银行、兴业银行
- 消费：美的集团、伊利股份、格力电器
- 医药：恒瑞医药、云南白药、迈瑞医疗
- 科技：海康威视、长江电力、中国中免、立讯精密

**为什么选龙头？** 流动性好（买卖容易成交），数据质量高，不容易被庄家操控。

---

### ⚙️ 配置文件更新

`config/config.yaml` 新增：
```yaml
# 选股扫描参数
screener:
  top_n: 10              # 最终输出前 N 只
  min_tech_score: 40.0   # 技术面低于 40 分的不入选
  max_position_pct: 20.0 # 单股最大仓位 20%
```

---

## v1.0.1（2026-06-27）

### 🏗️ 项目整合：从散落脚本到完整系统

**一句话：** 把之前零散的策略脚本、回测代码、自动化模块整合成一个结构清晰的完整项目。

**项目结构：**
```
XiaoLiangTrader/
├── main.py              ← 所有命令都在这（--backtest / --train / --once / --stop）
├── config/              ← 配置文件（改参数不用改代码）
├── data/                ← 数据获取（akshare，免费无需注册）
├── strategy/            ← 策略（双均线 + ML 增强）
├── ml_model/            ← 机器学习（LightGBM/XGBoost/sklearn）
├── backtest/            ← 回测引擎（Backtrader）
├── bot/                 ← 自动交易（执行器 + 风控 + 通知）
└── utils/               ← 工具（日志 + 加密）
```

---

### 📈 核心策略：双均线交叉 + 成交量过滤

**新增文件：**
- `strategy/signals.py` — 信号定义（BUY/SELL/HOLD）
- `strategy/dual_ma.py` — 双均线策略

**买入条件（同时满足）：**
1. MA5 上穿 MA20（金叉）— 短期趋势向上
2. 当日成交量 > 20日均量 × 1.5 — 有人在买

**卖出条件（任一触发）：**
1. MA5 下穿 MA20（死叉）— 趋势反转
2. 亏损达 -8%（止损）— 认输离场
3. 盈利达 +15% 后从最高点回撤 5%（跟踪止盈）— 锁定利润

---

### 🤖 ML 增强：用机器学习过滤假信号

**新增文件：**
- `strategy/ml_strategy.py` — ML 增强策略
- `ml_model/features.py` — 30 个技术指标特征
- `ml_model/predictor.py` — 统一预测器（支持 4 种模型）

**为什么加 ML？** 双均线策略在震荡市会频繁"假金叉"→ 买入 → 止损，亏钱。
ML 的作用是：只在模型认为"大概率涨"时才买入，过滤掉假信号。

**支持 4 种模型（config.yaml 中 `ml.model_type`）：**

| 模型 | 说明 | 需要安装 |
|------|------|----------|
| `lightgbm` | 速度最快，推荐 | `pip install lightgbm` |
| `xgboost` | 精度略高 | `pip install xgboost` |
| `random_forest` | 最稳定，不过拟合 | sklearn 自带 |
| `gradient_boosting` | sklearn 自带 GBDT | sklearn 自带 |

**30 个特征：**
- 均线偏离率（价格离均线多远）
- 1/5/10/20 日收益率（涨了多少）
- 波动率、ATR（波动大不大）
- 量比、量价相关性（有没有人买卖）
- RSI、MACD、KDJ、布林带（经典技术指标）
- K线形态（上下影线、实体大小）
- 趋势斜率（整体向上还是向下）

---

### 📉 回测引擎

**新增文件：**
- `backtest/engine.py` — Backtrader 回测

**用法：**
```bash
python main.py --backtest
```

**输出：**
- 年化收益率
- 胜率（盈利交易占比）
- 最大回撤（最惨的时候亏了多少）
- 夏普比率（收益/风险，越高越好）
- Calmar比率（年化/回撤）
- 交易明细表
- K线图（保存到 `output/backtest_result.png`）

---

### 🤖 自动化交易

**新增文件：**
- `bot/executor.py` — 模拟盘执行器（SQLite 持久化）
- `bot/risk.py` — 风控（Kill Switch + 涨跌停 + 仓位）
- `bot/notifier.py` — 邮件通知
- `bot/scheduler.py` — 每日自动编排

**模拟盘特性：**
- 佣金万2.5 + 印花税千一 + 0.1% 滑点
- A股100股整数
- T+1限制（今天买的今天不能卖）
- 全部存 SQLite，重启不丢状态

**风控机制：**
- **Kill Switch**：创建 `.kill_switch` 文件或运行 `--stop` 立即停止
- **涨跌停**：涨停不追（≥9.5%），跌停不卖（≤-9.5%）
- **止损**：单笔 -8% 强制卖出
- **跟踪止盈**：盈利 15% 后从最高点回撤 5% 卖出
- **仓位限制**：单股 ≤ 20%，总仓位 ≤ 60%
- **日亏损限额**：当日 ≥ 3% 暂停交易

---

### 🔐 安全

**新增文件：**
- `utils/crypto.py` — AES 加密
- `utils/logger.py` — 日志（按日轮转30天）

**密钥保护：**
```bash
# 加密 config.yaml 中的密码和 API Key
python main.py --encrypt
# 之后 password: "abc123" 变成 password: "ENC:gAAAAA..."
```

---

### 📧 邮件通知

支持 QQ邮箱 / 163 / Gmail，交易信号、异常、每日报告自动推送。

```yaml
# config.yaml
email:
  enabled: true
  smtp_server: "smtp.qq.com"
  sender: "你的邮箱"
  password: "授权码"      # --encrypt 后自动加密
  receiver: "接收通知的邮箱"
```

---

### ⚠️ 已知限制

1. **实盘接口未实现**：当前只有模拟盘，实盘需继承 `bot/executor.py` 的 `BaseExecutor` 自行对接券商
2. **数据依赖网络**：首次运行需要联网从 akshare 下载数据（免费），之后走本地缓存
3. **无 GUI**：全部命令行操作，适合有编程基础的用户

---

## 版本号说明

- **v1.0.x** — 小版本，修 bug、加功能
- **v1.x.0** — 大版本，架构调整
- **v0.x.x** — 早期实验版

---

**仅供学习，不构成投资建议。** 🎓
